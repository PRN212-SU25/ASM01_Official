<Window x:Class="GROUP04WPF.Views.ReportView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Booking Report" Height="500" Width="600">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="10">
            <TextBlock Text="Start Date:" Margin="0,0,10,0" VerticalAlignment="Center" />
            <DatePicker SelectedDate="{Binding StartDate}" Margin="0,0,10,0" />
            <TextBlock Text="End Date:" Margin="0,0,10,0" VerticalAlignment="Center" />
            <DatePicker SelectedDate="{Binding EndDate}" Margin="0,0,10,0" />
            <Button Content="Generate" Command="{Binding GenerateReportCommand}" />
        </StackPanel>
        <DataGrid Grid.Row="1" ItemsSource="{Binding Bookings}" AutoGenerateColumns="False" Margin="10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding BookingReservationID}" />
                <DataGridTextColumn Header="Booking Date" Binding="{Binding BookingDate, StringFormat=yyyy-MM-dd}" />
                <DataGridTextColumn Header="Total Price" Binding="{Binding TotalPrice}" />
                <DataGridTextColumn Header="Customer ID" Binding="{Binding CustomerID}" />
                <DataGridTextColumn Header="Status" Binding="{Binding BookingStatus}" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>