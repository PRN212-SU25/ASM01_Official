<Window x:Class="GROUP04WPF.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="FU Mini Hotel System" Height="300" Width="400">
    <Grid>
        <StackPanel Margin="20">
            <TextBlock Text="Sign in" 
          FontSize="22" 
          FontWeight="Bold" 
          TextAlignment="Center" 
          Foreground="DarkBlue"
          Margin="0,0,0,20"/>
            <TextBlock Text="Email:" />
            <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" />
            <TextBlock Text="Password:" Margin="0,10,0,0" />
            <!--<PasswordBox x:Name="passwordBox" />-->
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="158*" />
                    <ColumnDefinition Width="5*"/>
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <!-- PasswordBox khi ẩn mật khẩu -->
                <PasswordBox x:Name="passwordBox" Grid.Column="0" Visibility="Visible" Grid.ColumnSpan="2" />
                <!-- TextBox khi hiện mật khẩu -->
                <TextBox x:Name="passwordTextBox" Grid.Column="0" Visibility="Collapsed" Grid.ColumnSpan="2" />
                <!-- Nút con mắt -->
                <Button x:Name="togglePasswordButton" Grid.Column="2" Content="👁" Width="30" Margin="5,0,0,0" Click="TogglePasswordButton_Click" />
            </Grid>
            <Button Content="Login" Command="{Binding LoginCommand}" Margin="0,20,0,0" />
        </StackPanel>
    </Grid>
</Window>