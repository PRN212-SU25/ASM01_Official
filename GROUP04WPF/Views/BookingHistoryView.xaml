<Window x:Class="GROUP04WPF.Views.BookingHistoryView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Booking History" Height="400" Width="600">
    <Grid>
        <DataGrid ItemsSource="{Binding Bookings}" AutoGenerateColumns="False" Margin="10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding BookingReservationID}" />
                <DataGridTextColumn Header="Booking Date" Binding="{Binding BookingDate, StringFormat=yyyy-MM-dd}" />
                <DataGridTextColumn Header="Total Price" Binding="{Binding TotalPrice}" />
                <DataGridTextColumn Header="Status" Binding="{Binding BookingStatus}" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>